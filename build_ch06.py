# -*- coding: utf-8 -*-
"""第 6 章 液压控制系统设计 —— 内容构建（WIP，逐节扩充）。Produces parts/ch06.docx."""
import os, re, fitz, hashlib, numpy as np
from hsbook_docx import DocBuilder, extract_equations, SRC_PDF, pidx

CH = 6
PAGES = range(212, 289)
os.makedirs("ch6_eqs", exist_ok=True)
doc = fitz.open(SRC_PDF)
EQ = {}
for pp in PAGES:
    EQ.update(extract_equations(SRC_PDF, pidx(pp), CH, "ch6_eqs"))

def _band(page, y0, y1, label, dpi=200, pad=7):
    ws = page.get_text("words"); L = min(w[0] for w in ws); R = max(w[2] for w in ws)
    box = fitz.Rect(L-pad, y0, R+pad, y1)
    path = os.path.join("ch6_eqs", f"eq_{label}.png"); page.get_pixmap(dpi=dpi, clip=box).save(path)
    return (path, box.width/72*2.54)
EXPLICIT_EQ = {"6.100": (249, 163, 228)}   # OCR-invisible label -> explicit band
for lab, (pg, y0, y1) in EXPLICIT_EQ.items():
    EQ[lab] = _band(doc[pidx(pg)], y0, y1, lab)

# ---- figures ----
os.makedirs("ch6_figs", exist_ok=True)
FIGPAGE = {"6.1": 214, "6.2": 216, "6.3": 217, "6.4": 219, "6.5": 221, "6.6": 223,
           "6.7": 225, "6.8": 225, "6.9": 227, "6.10": 229, "6.11": 230, "6.12": 230,
           "6.13": 231, "6.14": 232, "6.15": 233, "6.16": 234, "6.17": 236,
           "6.18": 237, "6.19": 242, "6.20": 244, "6.21": 247, "6.22": 248,
           "6.23": 253, "6.24": 254, "6.25": 257, "6.26": 258, "6.27": 259,
           "6.28": 260, "6.29": 261, "6.30": 264, "6.31": 265}
MANUAL  = {"6.1": (214, 65, 309, 388, 399), "6.6": (223, 65, 80, 388, 230),
           "6.9": (227, 65, 125, 388, 298), "6.12": (230, 65, 375, 388, 478),
           "6.13": (231, 65, 510, 388, 607), "6.16": (234, 65, 425, 388, 495),
           "6.18": (237, 70, 273, 388, 443), "6.19": (242, 70, 298, 388, 416),
           "6.21": (247, 70, 368, 388, 458), "6.23": (253, 65, 228, 388, 425),
           "6.25": (257, 65, 53, 388, 242), "6.26": (258, 65, 328, 388, 458),
           "6.27": (259, 65, 368, 388, 490), "6.28": (260, 65, 88, 388, 335)}
TOPCUT  = {}
def _figbox(page, fig):
    ws = page.get_text("words"); left = min(w[0] for w in ws); right = max(w[2] for w in ws)
    blocks = sorted(page.get_text("blocks"), key=lambda b: b[1]); caps = {}
    for b in blocks:
        m = re.match(r"\s*Figure\s*(\d+\.\d+)\.", b[4].replace("\n", " "))
        if m: caps.setdefault(m.group(1), b)
    cy0 = caps[fig][1]; top = TOPCUT.get(fig, page.rect.y0 + 40)
    if fig not in TOPCUT:
        for b in blocks:
            if b[3] <= cy0-2 and len(b[4].strip()) > 55 and not re.match(r"\s*Figure\s*\d+\.\d+\.", b[4].replace("\n"," ")):
                if b[1] < page.rect.y0+30: continue
                if b[3] > top: top = b[3]
        for f2, b2 in caps.items():
            if f2 != fig and b2[3] <= cy0-2 and b2[3] > top: top = b2[3]
    return fitz.Rect(left-4, top+3, right+4, cy0-1)
def _crop(fig):
    if fig in MANUAL:
        p, x0, y0, x1, y1 = MANUAL[fig]; box = fitz.Rect(x0, y0, x1, y1); page = doc[pidx(p)]
    else:
        page = doc[pidx(FIGPAGE[fig])]; box = _figbox(page, fig)
    path = f"ch6_figs/fig_{fig}.png"; page.get_pixmap(dpi=200, clip=box).save(path); return path

FIGCAP = {
 "6.1": ("HSS 的一般控制结构", "General control structure for HSSs", 13),
 "6.2": ("带输入扰动 z 的标准闭环框图", "Block diagram of a standard closed loop with input disturbance z", 12),
 "6.3": ("含负载压力反馈回路的位置反馈", "Position feedback including a load pressure feedback loop", 11),
 "6.4": ("含加速度反馈回路的位置反馈", "Position feedback including an acceleration feedback loop", 11),
 "6.5": ("引入切换积分器", "Inclusion of a switching integrator", 8),
 "6.6": ("HSS 的线性观测器型状态反馈控制", "Linear observer-based state feedback control for HSSs", 12.5),
 "6.7": ("良好稳定域的定义", "Definition of nice stability region", 10),
 "6.8": ("HSS 状态反馈的典型极点配置", "Typical pole constellations for state feedback of HSSs", 12.5),
 "6.9": ("含增广积分作用的线性观测器型状态反馈控制（PI 状态控制器）", "Linear observer-based state feedback control with augmented integral action (PI state controller)", 13),
 "6.10": ("用于扰动补偿的前馈控制原理", "Principle of feedforward control for disturbance compensation", 11),
 "6.11": ("通用前馈控制方案", "Universal feedforward control scheme", 11),
 "6.12": ("位置反馈控制与负载压力前馈控制相结合", "Position feedback control combined with load pressure feedforward control", 11),
 "6.13": ("位置反馈控制与速度前馈控制相结合", "Position feedback control combined with velocity feedforward control", 11),
 "6.14": ("基于灰箱模型的自适应状态反馈控制方案", "Adaptive state feedback control scheme based on grey-box models", 12.5),
 "6.15": ("加入偏置校正", "Addition of an offset correction", 8.5),
 "6.16": ("阀曲线及其补偿曲线（Nissing, 2002）", "Valve curve and its compensation curve (Nissing, 2002)", 12),
 "6.17": ("固有频率作为活塞位置（按最大行程归一化）和面积比 α 的函数", "Natural frequency as a function of piston position and area ratio α", 12),
 "6.18": ("用节流-单向阀对差动缸进行速度补偿（Lemmen, 1995）", "Velocity compensation for differential cylinders using throttle-check valves (Lemmen, 1995)", 10),
 "6.19": ("经输入-输出线性化变换后的同步缸模型（含非线性摩擦和非线性流函数）", "Input-output linearised synchronising cylinder model with non-linear friction and flow function", 13),
 "6.20": ("输入-输出线性化过程的框图", "Block diagram of input-output linearisation procedure", 13),
 "6.21": ("带极点配置和线性反馈控制器的输入-输出线性化", "Input-output linearisation with pole placement and linear feedback controller", 12.5),
 "6.22": ("基于反馈线性化的完整控制系统", "Complete control system based on feedback linearisation", 11.5),
 "6.23": ("Stribeck 曲线的近似", "Approximation of Stribeck curve", 12),
 "6.24": ("基于反馈线性化的双线性控制系统方案（Yin, 1994）", "Scheme of bilinear control system based on feedback linearisation (Yin, 1994)", 12),
 "6.25": ("级联负载力（FL）控制结构（Bernzen, 1999）", "Cascade load force (FL) control structure (Bernzen, 1999)", 13),
 "6.26": ("基于输入-输出线性化的级联负载力控制算法小结", "Summarised algorithm of cascade load force control based on input-output linearisation", 12.5),
 "6.27": ("完整级联 FL 控制：内环控制执行器压差、外环控制负载运动（Heintze, 1997；van Schothorst, 1997）", "Complete cascade FL control with inner-loop pressure-difference and outer-loop load-motion control", 13),
 "6.28": ("在控制中利用模糊逻辑的一些例子（本节讨论阴影块对应的例子）", "Some examples for utilising fuzzy logic in control", 13),
 "6.29": ("用自适应设定点滤波器和模糊比例状态控制器对由模糊状态空间模型描述的对象进行控制的概念", "Control concept with adaptive set-point filter and fuzzy proportional state controller", 13),
 "6.30": ("带状态重构和自适应设定点滤波器的模糊状态反馈控制器算法小结", "Summarised algorithm of the fuzzy state feedback controller with state reconstruction and adaptive set-point filter", 13),
 "6.31": ("离散时间系统的“良好稳定”域（Kroll et al., 2000）", "Area of 'nice stability' of discrete-time systems (Kroll et al., 2000)", 10.5),
}

b = DocBuilder()
_lasteq = [None]
def eq(*labels):
    for L in labels:
        entry = EQ[L]
        hh = hashlib.md5(open(entry[0], "rb").read()).hexdigest()
        if hh == _lasteq[0]: continue
        b.eq_image(entry); _lasteq[0] = hh
def fig(num):
    cn, en, w = FIGCAP[num]; b.figure(_crop(num), num, cn, en, w)

b.h1("第 6 章　液压控制系统设计", "CHAPTER 6  HYDRAULIC CONTROL SYSTEMS DESIGN",
     page_break_before=True)
b.para("本章的目的是综述 HSS 的基础与高级控制设计方法，包括理论背景以及对第 4、5 章所导出"
       "精选模型的应用。本章回顾标准控制设计方法的优点与局限，并利用控制理论领域的最新进展"
       "（涉及非线性控制、“经典”控制和智能控制技术，特别是模糊控制）应用于 HSS。每种设计"
       "方法——无论经典还是现代——都有优缺点和长短处。本章论述背后的理念是：设计者需理解"
       "所有这些，才能以最小代价开发出令人满意的设计。然而须指出，本章无法涵盖所有现有线性和"
       "非线性控制策略的每个细节。")

b.h2("6.1　引言", "Introduction")
b.para("与其他工业控制系统类似，液压控制系统的完整设计过程通常包含以下步骤：")
b.para("1.　建模。　由物理方程（白箱建模，见第 4 章）、或用辨识技术由实验数据（黑箱建模，见"
       "第 5 章）、或两种方法的结合（灰箱建模，见第 5 章）确定 HSS 的数学模型。", indent=False)
b.para("2.　输入-输出可控性分析。　考察可期望的（最大）闭环性能，有助于决定初始控制结构。", indent=False)
b.para("3.　控制结构选择。　在此阶段选择待操纵和待测量的变量，以及它们之间应建立的联系。", indent=False)
b.para("4.　控制器设计。　包括把工程设计问题转化为数学设计问题的表述，以及相应控制器的综合。", indent=False)
b.para("5.　控制系统分析。　通过分析和仿真，对照性能指标或设计者经验评估控制系统。", indent=False)
b.para("6.　控制器实现。　在此阶段把控制器算法编码（计算机控制时用软件），注意处理抗积分饱和"
       "（anti-windup）、无扰切换（bump-less transfer）和物理约束等重要问题。", indent=False)
b.para("7.　控制系统调试与整定。　最后使控制器上线，进行现场测试并实施任何所需修改（即控制器"
       "参数的整定与优化），然后认证受控 HSS 完全可运行并满足性能指标。", indent=False)
b.para("本章主要聚焦于步骤 4、5 和 6。对其他步骤感兴趣的读者可参阅 Skogestad 与 Postlethwaite"
       "（1996）和 Åström 与 Wittenmark（1997）的教材。")
b.para("就 HSS 而言，其控制受到相当大的关注，因为它们在运行中以高功率水平提供高性能运动"
       "控制，以及闭环位置、速度和力控制系统。因此 HSS 在许多工业领域有广泛应用，如机床、"
       "机器人、运动模拟器、疲劳试验系统、轧机等。然而，HSS 的动态特性复杂且高度非线性，主要"
       "非线性包括液压油的可压缩性、伺服阀复杂的流动性质以及液压执行器中的摩擦。此外，液压"
       "系统的工况及作用其上的扰动以复杂方式变化（如阀、油和负载参数可能显著变化），且这些"
       "参数因种种原因（如温度相关行为）常常并不精确已知或时变。所有这些性质和事实使控制设计"
       "和整定变得困难。")
b.para("液压伺服系统闭环控制的主要目标是：")
b.bullet("线性化的输入-输出行为，且在整个运行范围内保持一致。")
b.bullet("足够的阻尼，以获得更好的阶跃响应。")
b.bullet("尽可能（在液压系统动态和由未建模动态、参数变化与扰动所施加的鲁棒稳定性要求允许的"
         "范围内）改善控制带宽。")
b.bullet("机械部件的尺寸和流量至少应保持不变。")
b.para("因此，理想控制器应对参数和扰动变化鲁棒，并同时带来最佳性能。然而实践中须视手头应用"
       "权衡取舍。许多工业 HSS 控制器通过增大缸径以提高缸中流体的有效刚度，从而以固定增益"
       "控制律实现高带宽；这需要更大、更昂贵的部件和更高的流量才能以给定速度移动负载。获得"
       "快速响应的更好方法是对系统的主导动态建模，然后在控制器设计中利用这一动态知识——其"
       "优点是：要达到给定带宽，机械部件更小、所需流量更少，因此整个系统便宜得多（Bobrow "
       "and Lum, 1996）。")
b.h3("6.1.1　一般方法", "General Approaches")
b.para("尽管在大多数应用中 HSS 的控制问题可描述为控制活塞运动的问题（液压执行器应施加该运动"
       "所需的力），但可考虑图 6.1 所示的广义控制结构，其中可识别出三个部分（van Schothorst, "
       "1997）：")
b.bullet("轨迹生成。　这一部分（可能由某外部驱动信号 r 馈入）为运动系统生成期望轨迹。在许多"
         "（工业）应用中，轨迹生成器（或预滤波器）只生成系统须跟随的期望位置轨迹 xref；更高级的"
         "轨迹生成器（如机器人操作臂中）还提供可用于前馈部分的期望速度 ẋref 和加速度 ẍref。")
b.bullet("前馈控制。　有了期望轨迹，前馈控制可用于生成供给（反馈）控制系统的信号，使受控运动"
         "系统尽可能好地实现期望轨迹。实际上，最优前馈控制使用系统的理想逆模型，恰好生成实现"
         "期望轨迹的那些控制信号。反馈部分是必不可少的，因为不可能实现运动系统的精确逆模型、"
         "也不可能知道所有扰动。")
b.bullet("反馈控制。　反馈控制可看作校正控制，用以应对前馈通路中的（建模）误差和未知扰动。它"
         "把前馈信号与系统测量输出信号的反馈结合起来。")
fig("6.1")
b.para("轨迹生成器通常只是一个二阶低通滤波器，如由外部参考信号 r(t)（通常是位置参考）驱动、"
       "具有可调截止频率 ωe 的巴特沃思滤波器（见 5.5.3.1 节）：")
eq("6.1")
b.para("借助 ωe，可调整期望位置-速度-加速度轨迹的频率内容。在高频范围，期望加速度与 ωe² 成"
       "正比；因此从物理上看，高截止频率 ωe 意味着对系统的高要求。", indent=False)
b.h3("6.1.2　文献概览与分类", "Literature Scan and Classification")
b.para("在液压执行器控制领域，可找到种类繁多的控制设计技术和应用。要对文献中的控制问题和控制"
       "设计技术作出完整的概览和分类并非易事。一般而言，液压伺服控制问题被处理为：")
b.bullet("位置控制问题（Merritt, 1967；Viersma, 1980；……；Nissing, 2002）。这是液压伺服控制的"
         "标准应用。")
b.bullet("速度控制问题（Anderson, 1988；Backe, 1992；……）。速度控制一般用于旋转驱动应用，但也"
         "常作为速度补偿隐含包含在位置控制方案中。")
b.bullet("力控制问题（Sepehri 等, 1990；Backe, 1992；Boes, 1995；Heintze and van der Weiden, "
         "1995）。这通常见于金属成形机和试验机。")
b.para("尽管不同类型控制问题有这种清晰区分，但很难就用于解决所提控制问题的控制设计技术给出"
       "刻画。实际上，可用计算能力、控制设计者的经验与偏好、可用传感器等实际原因似乎在选择"
       "某种控制设计策略时起重要作用（van Schothorst, 1997）。尽管如此，表 6.1 给出液压伺服控制"
       "文献的一个适度分类和简要综述。")
b.label("表 6.1　HSS 控制技术文献概览")
b.table(["控制技术", "精选文献"],
        [["经典反馈控制", "Merritt (1967), Viersma (1980), Anderson (1988), Backe (1992)"],
         ["状态反馈控制", "von Wierschem (1981), Feuser (1984), Kockemann 等 (1991), Neumann 等 (1991a,b)"],
         ["反馈控制的扩展（前馈控制、非线性补偿）", "McClamroch (1985), de Boer (1992), Lierschaft (1993), Heintze and van der Weiden (1995), Bobrow and Lum (1996), Polzer and Nissing (2000b), van Schothorst (1997)"],
         ["预测控制", "Kotzev 等 (1994), Stahl and Irle (1999)"],
         ["自适应控制（自校正/学习控制）", "Porter and Tatnall (1970), Yun and Cho (1985, 1988, 1991), Saffe (1986), Kockemann 等 (1991), Boes (1992, 1995), Huang and Wang (1995), Bobrow and Lum (1996) 等"],
         ["变结构控制（滑模控制）", "Lee and Lee (1990), Chern and Wu (1992), Hwang and Lan (1994), Behmenburg (1995)"],
         ["反馈线性化（输入-输出线性化）", "Hahn 等 (1994), Del Re and Isidori (1995), Alleyne (1996), Bernzen (1999a), Sohl and Bobrow (1999), Lemmen 等 (2000), Tunay 等 (2001)"],
         ["模糊控制", "Klein (1993), Behmenburg (1995), Boes (1995), Zhao and Virvalo (1995), Berger (1997)"],
         ["神经控制", "Burton 等 (1992), Plummer and Vaughan (1996)"],
         ["双线性控制", "Naujoks and Wurmthaler (1988), Guo and Schwarz (1989), Guo (1991), Yin (1992, 1994), Schwarz 等 (1996)"]],
        [5.5, 10.5])

b.h2("6.2　经典反馈控制设计", "Classical Feedback Control Design")
b.para("控制液压伺服系统的标准方法是应用线性反馈控制设计方法。这些方法的理论背景可见若干"
       "入门控制书籍，如 Franklin 等（1986）、Föllinger（1990）、Unbehauen（1993, 1994）和 Lunze"
       "（2001）；高级主题另参阅 Franklin 等（1990）、Skogestad 与 Postlethwaite（1996）、Åström 与 "
       "Wittenmark（1997）等。本节仅以紧凑方式给出一些精选设计技术。")
b.para("我们从回顾闭环控制原理（已在 2.4.1 节、图 2.12 引入）开始，设 Gp(s) 为待控系统的（线性）"
       "传递函数，Gc(s) 为控制器的传递函数（图 6.2）。则对输入 yref 的闭环传递函数为：")
fig("6.2")
eq("6.2")
b.para("对扰动 z 的传递函数可写为", indent=False)
eq("6.3")
b.para("线性系统允许叠加，即输出 y 是参考和扰动两个输入的共同贡献。包含两个输入的结果为", indent=False)
eq("6.4")
b.para("在大多数液压伺服控制的标准应用中，只有位置参考可用。这类应用的常规控制策略是对执行器"
       "位置进行比例反馈以获得伺服行为，同时多数情况下为阻尼目的引入比例压力反馈回路（Merritt, "
       "1967；Viersma, 1980）；见图 6.3。其目的是在存在扰动 z（负载或外力变化、供油压力变化等）"
       "时，使位置 x(t) 保持接近位置参考 xref(t)。")
fig("6.3")
b.para("为更深入了解这种经典控制器的有效性，考虑 4.4.4.3 和 4.4.4.4 节导出的液压系统线性化模型"
       "（为简单起见忽略阀动态）。由于图 4.26 的模型结构通常表现为一个（阻尼很差，即典型 "
       "Dh = 0.05–0.25 的）二阶系统与一个积分器串联，显然比例位置反馈导致跟踪行为。")
b.h3("6.2.1　压力反馈", "Pressure Feedback")
b.para("首先分析压力反馈回路的效果。应用压力反馈")
eq("6.5")
b.para("导出如下闭环传递函数：", indent=False)
eq("6.6")
b.para("它被化为一个 PT2 与积分器串联的标准形式（见式 4.244）。简单计算给出受控系统的固有频率 "
       "ωh,cl 和阻尼 Dh,cl", indent=False)
eq("6.7", "6.8")
b.para("只要黏性摩擦系数 σ 小（液压系统通常如此），式 6.7 和 6.8 表明压力反馈对固有频率 ωh,cl 仅"
       "有微弱影响。另一方面，压力反馈对阻尼 Dh,cl 有与泄漏项等效的作用。因此，压力控制系统的 "
       "Dh,cl 可用压力反馈轻易调整，而 ωh,cl 变化很小。", indent=False)
b.para("然而，液压执行器对扰动力表现出有限的刚度。由于压力反馈可视为人为引入的泄漏，它会增大"
       "如库仑摩擦或静摩擦对定位精度的扰动效应（Heintze, 1997）。为此，考虑扰动传递函数")
eq("6.9")
b.para("于是负载刚度可确定为", indent=False)
eq("6.10")
b.para("或等价地", indent=False)
eq("6.11")
b.para("由于此式右端第二项小于 1，负载刚度因压力反馈而降低。为避免该问题，应使用如下形式的"
       "压力反馈来代替式 6.5 的控制律：", indent=False)
eq("6.12")
b.para("若再次计算特征参数，可看出这种压力反馈的有效性：", indent=False)
eq("6.13", "6.14", "6.15")
b.para("因此，只有液压系统的阻尼受压力反馈增益 KpL 影响。", indent=False)
b.h3("6.2.2　加速度反馈", "Acceleration Feedback")
b.para("由于加速度与压差密切相关（见式 4.257），液压阻尼 Dh,cl 也可经加速度反馈调整；见图 6.4。"
       "此时可导出")
fig("6.4")
eq("6.16")
b.para("ωh,cl 和 Ch,cl 分别同式 6.13 和 6.15。因此，加速度反馈也不会恶化液压驱动系统的刚度。然而，"
       "测量加速度一般被认为比测量 pL 更复杂、因而更昂贵；故实际应用中常无传感器测量加速度。"
       "尽管如此，加速度可由位置或速度信号微分估计（见 6.10.1.3 节），但由于位置信号有噪声以及"
       "微分引入的相位延迟，所构造的加速度信号质量不会很理想。", indent=False)

b.h3("6.2.3　位置反馈", "Position Feedback")
b.para("接下来，为实现位置跟踪行为，施加对位置 xp 的反馈。该反馈主要把原点处的极点移入左半"
       "平面，并对共轭复极点对的位置有一定影响（其阻尼首先由压力反馈调整）。位置控制有许多"
       "选择，如简单的 P 控制。为同时提供稳定性和足够阻尼，位置反馈增益 Kp 应选得满足所谓的 "
       "M=1.3 准则（Viersma, 1980）：即闭环幅值（比）绝不超过 1.3（称为频率响应谐振峰）。遵守"
       "该准则，闭环阶跃响应通常显示约 23% 的超调（对二阶系统精确成立）。按良好工程实践，"
       "M=1.3 准则导致约为 2 的幅值裕度和约 45° 的相位裕度（PM）。注意，二阶系统的相位裕度与"
       "阻尼比的关系为 D ≈ PM/100（Franklin 等, 1990）。")
b.para("还要注意，若使用 PT1 控制器（一阶滞后），即")
eq("6.17")
b.para("或 PPT1 控制器，即", indent=False)
eq("6.18")
b.para("则可获得更快的闭环阶跃响应。常规线性控制器对位置控制、速度控制和力控制的适用性概览"
       "见表 6.2（另见 Anderson, 1988）。对某些液压伺服系统应用，可使用 PI 控制器以提高低频"
       "开环增益并减小稳态负载误差及“加速度”误差（Viersma, 1980）；然而须仔细考虑积分时间"
       "常数，因为此时闭环传递函数含双重积分器，会引起稳定性问题。", indent=False)
b.label("表 6.2　线性控制器在位置/速度/力控制上的性能概览（从 −− 最差 到 ++ 最佳）")
_t62 = doc[pidx(221)].get_pixmap(dpi=200, clip=fitz.Rect(65, 113, 388, 211))
_t62.save("ch6_figs/table_6.2.png")
b.image_block("ch6_figs/table_6.2.png", 15.5)
b.para("一种实用的折中是使用切换积分器（switching integrator），它只在控制器输入零点附近的区间 "
       "[eu, eo] 内被激活；见图 6.5。")
fig("6.5")
b.h3("6.2.4　小结", "Summary")
b.para("反馈增益 Kp 和 KpL 的选择分两步进行：")
b.para("1.　整定压差反馈增益 KpL，使所得闭环系统（位置反馈仍为零）的阻尼比 Dh 处于区间 "
       "0.5 < Dh < 0.7 内的某处，视实际应用和期望系统特性而定。", indent=False)
b.para("2.　此后增大位置反馈增益 Kp 以获得位置跟踪，例如直到满足所谓的 M=1.3 准则（Viersma, "
       "1980）。另一良好做法是选择比例增益，使受控系统的频率 ωh,cl 处于区间 1.3 ωh,open < ωh,cl < "
       "1.7 ωh,open 内。", indent=False)
b.para("这里强调，该控制策略的主要重点是位置控制（只有位置参考 xref 可用），压差反馈只用于"
       "修改闭环动态（阻尼）。压力反馈增益不应太大，否则会导致带宽受限的过阻尼系统。若可用，"
       "加速度反馈更可取，因为它在改善阻尼的同时不恶化对力扰动的刚度。注意，按上述洞见，对"
       "性能合理的 HSS 整定控制器是一项直截了当的任务，无需如实践中通常那样的特定参数化模型；"
       "然而，用该方法不可能任意配置极点以得到 HSS 某种所要求的受控行为。")
b.h2("6.3　基于估计器的状态反馈控制", "Estimator-based State Feedback Control")
b.para("控制理论中众所周知，通过全状态反馈可任意配置系统的所有极点以迫使系统呈现期望行为。"
       "因此，在前一小节的位置控制方案中加入速度反馈和压力（或压差）反馈，便可把液压伺服系统"
       "的闭环极点配置到期望位置。该方法已成功用于不同应用（von Wierschem, 1981；Feuser, "
       "1984；Kockemann 等, 1991）。状态空间设计方法的一个吸引人之处是其步骤如下：")
b.para("1.　在所有状态向量元素可用、且系统暂无参考信号（yref=0）的假设下设计控制律。", indent=False)
b.para("2.　设计一个估计器（也称观测器），当提供某些状态的测量时，它估计整个（或部分）状态"
       "向量。", indent=False)
b.para("3.　把控制律与估计器结合，其中控制律计算基于估计状态而非实际状态。", indent=False)
b.para("4.　加入预滤波器或积分器，以消除比例状态控制器情形下的稳态误差，或抑制作用于系统的"
       "未知扰动效应。", indent=False)
b.para("图 6.6 展示了对 HSS（考虑线性化模型式 4.234），控制律与估计器如何配合。")
fig("6.6")
b.h3("6.3.1　状态控制律的计算", "Computation of the State Control Law")
b.para("考虑如下形式的线性时不变（LTI）状态方程：")
eq("6.19")
b.para("确定这类系统状态反馈律有两种常见可能：极点配置法和最优控制法。后者也即著名的线性二次"
       "调节器（LQR）设计法。", indent=False)
b.label("6.3.1.1　极点配置（Pole Assignment）")
b.para("第一步是暂时假设 yref=0 并寻找如下形式的简单反馈律")
eq("6.20")
b.para("把控制律式 6.20 代入式 6.19，得", indent=False)
eq("6.21")
b.para("于是闭环系统的特征方程为", indent=False)
eq("6.22")
b.para("控制律设计就是选取增益向量 k，使式 6.22 的根处于期望位置，即 s = s1, s2, …, sn，使", indent=False)
eq("6.23")
b.para("因此，k 的所需元素通过匹配式 6.22 和 6.23 的系数确定。当且仅当系统（完全）可控（即"
       "可控性矩阵", indent=False)
eq("6.24")
b.para("满秩 n）时，用状态反馈把系统极点移到任何期望位置才是可能的。求解反馈增益的另一方法是"
       "使用 Ackermann 公式（Ackermann, 1972）", indent=False)
eq("6.25")
b.para("其中 C(F) 定义为", indent=False)
eq("6.26")
b.para("ai 是期望特征多项式（式 6.23）的系数。", indent=False)
b.label("6.3.1.2　最优控制（Optimal Control）")
b.para("时间连续 LTI 系统（式 6.19）的线性二次调节器设计（LQR）计算最优增益向量 k，使状态反馈"
       "律（式 6.20）最小化代价函数")
eq("6.27")
b.para("其中 Q 为适当选择的半定矩阵，R 为正定矩阵（因考虑 SISO 系统，R 在此为标量）。最优增益"
       "向量 k 由下式给出", indent=False)
eq("6.28")
b.para("其中 S 是代数 Riccati 矩阵方程的解", indent=False)
eq("6.29")
b.para("设计参数是矩阵 Q 和 R。Q 通常取为对角，常设为单位矩阵；R 用于惩罚控制作用。改变加权"
       "矩阵元素间的相对大小意味着恢复速度与控制信号幅值之间的权衡。寻找相对权重的过程很难，"
       "通常通过试错完成。", indent=False)
b.h3("6.3.2　极点位置的选择", "Selection of Pole Locations")
b.para("状态反馈增益的设计通常用标准设计方法（如上述极点配置）进行，由此可把极点配置成任意"
       "模式以塑造 HSS 的闭环动态，其主要重点是快速跟踪位置参考信号。一般推荐把极点配置在"
       "“良好稳定”区域内，该区域由以下边界包围（见图 6.7）：")
b.bullet("稳定边界（虚轴）减去安全裕度 ε > 0。")
b.bullet("两条恒定最小阻尼 Dmin = cos φmin 的直线。")
b.bullet("距稳定边界的最大距离 E > 0，以限制控制作用并抑制测量噪声。")
fig("6.7")
b.para("通常，闭环极点选为一对期望的主导二阶极点，其余极点选得其实部对应于足够阻尼的模态，"
       "使闭环系统以合理的控制代价模拟二阶系统（Franklin 等, 1986）。液压系统推荐的极点配置"
       "（这里针对式 4.250 或 4.252 中 n=3 的降阶线性模型）为：")
b.para("(a) 所有极点配置在实轴上同一位置（二项式形式）；见图 6.8a。闭环系统将显示最大阻尼 "
       "D=1，即无超调（所谓非周期情形）。", indent=False)
b.para("(b) 所有极点有相同实部，其中两个构成夹角 45° 的复极点对；见图 6.8b。二阶极点选得阻尼比 "
       "D=cos(45°)≈0.7，第三个极点有相同实部。", indent=False)
b.para("(c) 极点位于一个圆上；见图 6.8c。二阶极点如 (b) 选取，带实部的极点稍向左移，使闭环动态"
       "相应更快。", indent=False)
fig("6.8")
b.h3("6.3.3　稳态误差的消除", "Elimination of Steady-state Errors")
b.para("若系统是 1 型（积分型）或更高型（如位置控制的 HSS）且 yref 是阶跃，则无稳态误差，即 "
       "y(∞) = yss = yref,ss。对 0 型（比例型）系统（如速度、压力或力控制的 HSS），会有误差，因为"
       "维持系统于期望状态需要一定控制。为解决该问题，有两种常见方案：为参考信号设计预滤波器，"
       "或引入积分作用。")
b.label("6.3.3.1　预滤波器的设计（Design of Pre-filter）")
b.para("可设计预滤波器")
eq("6.30")
b.para("以消除稳态误差，即确保稳态值 y∞ 与 yref,∞ 相等。总控制律则为", indent=False)
eq("6.31")
b.para("于是得到闭环系统的状态空间描述（以 yref 为输入）", indent=False)
eq("6.32")
b.label("6.3.3.2　状态扩展的积分控制（“PI 状态反馈控制”）")
b.para("该方法的原理是用一个“人工”积分器扩展模型式 6.19，从而给现有输出添加一个积分输出。"
       "即用 xI 增广状态向量 x，xI 满足微分方程")
eq("6.33")
b.para("因此得到扩展系统模型", indent=False)
eq("6.34")
b.para("PI 状态控制律为", indent=False)
eq("6.35")
b.para("这将得到图 6.9 所示的控制结构。有了系统的这一扩展定义，6.3.1 节已给出的设计技术（极点"
       "配置和最优控制）可以类似方式用于确定 k（从而确定 kI 和 k1）。由于 kp 应选为（Föllinger, "
       "1990）", indent=False)
eq("6.36")
b.para("k 的计算便直截了当", indent=False)
eq("6.37")
b.para("PI 状态控制器能抑制作用于系统的未知扰动效应（如通过参数变化）。注意，观测器（或估计器）"
       "设计必须基于扩展模型式 6.34。", indent=False)
fig("6.9")
b.h3("6.3.4　对液压伺服系统线性模型的应用", "Application to HSS Linear Models")
b.para("形式上，状态反馈控制器可基于线性模型式 4.234 设计。然而这会比基于简化模型式 4.250"
       "（忽略阀动态）的控制器设计复杂得多。此时反馈律取如下形式")
eq("6.38")
b.para("给出闭环的系统矩阵（式 6.21）", indent=False)
eq("6.39")
b.para("应用式 6.22 和 6.23 导出控制器增益", indent=False)
eq("6.40", "6.41", "6.42")
b.para("如前，系数 αi 与具体应用中期望的极点位置相关。", indent=False)
b.h2("6.4　线性反馈控制的扩展", "Extensions to Linear Feedback Control")
b.para("迄今论述的控制器都基于线性反馈原理。一方面，使用反馈的主要目的是镇定不稳定系统并减小"
       "可能的扰动和模型不准确性的影响；然而，用反馈快速跟踪参考通常有一个副作用：控制器对"
       "噪声（实践中不可避免）变得高度敏感。另一方面，线性控制设计无法确保闭环系统在整个运行"
       "范围内有相同的动态行为。因此，线性反馈控制应与其他技术结合以在实践中取得满意结果。"
       "对迄今给出的（线性）反馈控制方法有若干（非线性）扩展，如应用前馈控制、增益调度或自适应"
       "方法，以及对特定非线性的补偿。")
b.h3("6.4.1　反馈与前馈相结合的控制", "Combined Feedback and Feedforward Control")
b.para("前馈控制是一种消除可测量扰动的控制方法。其基本思想是利用测得的扰动来预测扰动对过程"
       "变量的影响并提供合适的补偿作用。该方法的通用原理示于图 6.10。与反馈控制相比，前馈控制"
       "的优点是可在扰动影响变量之前施加校正作用，因此前馈控制允许在不影响系统稳定性的情况下"
       "快速跟踪参考。")
fig("6.10")
b.para("考虑分别把输出 y 与扰动 z 和控制 u 联系起来的传递函数 Gz(s) 和 Gu(s)（见 4.4.4.4 节）。"
       "扰动的“完美”抵消通过下式获得")
eq("6.43")
b.para("前提是能获得并物理实现逆 Gu⁻¹(s)。这当然可能不成立，如由于测量扰动先于其出现于过程而"
       "产生的时延；此时须改用合适的近似。假设对象和控制器稳定且所有扰动已知，前馈控制方案可"
       "扩展以产生“完美”控制：", indent=False)
eq("6.44")
b.para("由此给出", indent=False)
eq("6.45")
b.para("遗憾的是，Gu(s) 和 Gz(s) 绝非精确模型，扰动也绝非精确已知。此外常遇到高幅值的非常活跃"
       "的控制信号（若不滤波）。因此，尽管前馈控制能对扰动采取某些预见性作用，但它本身不能"
       "保证良好的控制质量，须与反馈控制结合以减小模型和（扰动）信号不确定性的影响，如图 6.11 "
       "所示。", indent=False)
fig("6.11")
b.label("6.4.1.1　压差前馈控制（Pressure-difference Feedforward Control）")
b.para("若压差参考可用，可把它前馈到压差反馈回路，如图 6.12 所示。压差前馈增益可计算为式 4.258 "
       "传递函数静态增益的倒数：")
eq("6.46")
fig("6.12")
b.para("由于同时提供位置参考 xref 和压差参考 PL,ref，反馈控制设计具有位置控制与压力控制之间权衡"
       "的性质。尤其当用于生成 PL 参考的模型知识相当准确时，可在此权衡中强调压力控制，从而增大"
       "压力反馈增益、代价是较小的位置反馈增益。PL 参考前馈的一个额外优点是：只要 Fext 的估计"
       "可用，它就允许补偿外力（见 6.10.1.3 节）。")
b.label("6.4.1.2　速度前馈控制（Velocity Feedforward Control）")
b.para("从位置伺服控制的角度考虑液压执行器的模型结构（图 4.26）可见，在单位反馈下，恒速的位置"
       "参考只能以恒定跟踪误差被跟踪（所谓 1 型系统）。从物理上看，恒速运动需要恒定的阀流量，"
       "这意味着反馈控制情形下的恒定跟踪误差（van Schothorst, 1997）。避免该跟踪误差的一个显然"
       "方法是应用速度前馈（当然只在速度参考 ẋref 可用时才可能）；见图 6.13。速度前馈增益 Kx,ff "
       "的设计直截了当，应选为式 4.255 传递函数静态增益的倒数：")
eq("6.47")
b.para("忽略摩擦项（即 σ=0），可简化为", indent=False)
eq("6.48")
fig("6.13")
b.para("速度前馈的一个重要副产品是：补偿活塞速度对质量平衡的影响，使压力动态与负载动态解耦；"
       "另见 6.6.2 节。")
b.h3("6.4.2　自适应控制", "Adaptive Control")
b.para("如前节所述，控制 HSS 的标准方法依赖于在工作点附近对动态作局部（雅可比）线性化，随后"
       "进行线性控制设计。然而，液压系统的工况和（负载）扰动以未知方式变化（如阀、油和负载"
       "参数可能显著变化，参数还可能因老化而改变）。因此，任何具有恒定反馈增益的常规控制器"
       "设计方法、或不考虑工况和负载扰动变化的现代控制设计方法，都无法获得最优性能，甚至可能"
       "发生不稳定的系统运行。为确保满意性能，当过程在不同工况和不同负载扰动下运行时，必须"
       "反复重新设计控制器参数。这直接引向自适应控制技术的应用，许多作者已为 HSS 提出并明确"
       "应用了它。5.7 节（图 5.22）的在线辨识方案现在可扩展用于自适应控制方案，如图 6.14 所示"
       "（另见 Boes, 1992, 1995）。当然，可用参数化黑箱模型（如 ARX 模型）代替灰箱（线性）"
       "模型（如 Yu and Kuo, 1997）。")
fig("6.14")
b.h3("6.4.3　特殊（静态）非线性的补偿", "Compensation of Special (Static) Non-linearities")
b.para("迄今给出的所有设计方法都未考虑液压驱动中存在的非线性效应，如库仑摩擦、间隙和非线性"
       "阀流特性。下面讨论如何给线性控制器添加某些非线性补偿以改善闭环系统的性能。非线性特性 "
       "αd,c2(x) 可通过在控制信号进入系统之前、在控制回路中植入该非线性的估计逆 αd,c2⁻¹(x) 来补偿。"
       "这假设非线性可由实验确定，或由任何模型或观测器估计。")
b.label("6.4.3.1　非理想阀几何的补偿（Compensation for Non-ideal Valve Geometry）")
b.para("可给控制量添加一个偏置校正项，以补偿死区行为（如因伺服阀的滞环、过遮盖或径向间隙，见 "
       "4.2.1.1 节）并减小稳态误差（图 6.15）。这种校正项的正确值可由反转流量-信号函数 Q(u)"
       "（4.5.1.1 节）通过实验确定；否则可从制造商样本中提取估计。")
fig("6.15")
b.para("特别地，大多数伺服阀有欠遮盖（闭式中位）或过遮盖（开式中位），尽管零遮盖是高度可取的。"
       "为在控制器设计中规避该问题，可使用 Polzer 与 Nissing（2000b）的阀补偿。为此，对静态"
       "情形（xp=0、ṗA=ṗB=0）测量输入电压 u 对应的活塞速度。借助测得的压力，可计算依赖于输入"
       "电压 u 的理论活塞速度。所需关系可由把 pA 和 pB 的压力动态方程 4.198 和 4.199 相减导出：")
eq("6.49")
b.para("它取决于运动方向，因为活塞表面积对正负速度不同。带过遮盖或欠遮盖的阀的典型输入-速度"
       "曲线见图 6.16（左）。为补偿过遮盖（从而确保式 4.198、4.199 和控制器设计有效），建立两"
       "曲线与零遮盖曲线 ẋp,th(u) 之差并用作阀补偿 uc。对给定输入电压 uunc（来自控制器），须添加"
       "额外补偿电压 uc，使理论活塞速度可由总输出达到：", indent=False)
eq("6.50")
fig("6.16")
b.label("6.4.3.2　非线性流特性的补偿（Compensation for Non-linear Flow Characteristic）")
b.para("文献中有这种补偿技术的例子，其中平方根表达式被反转并用于控制。Neumann 等（1991a,b）"
       "给出非线性仿真结果并得出结论：性能有相当大的改善。他们用观测的压差进行补偿，因为实际"
       "压差未被测量。Lierschaft（1993）以及后来的 Heintze 与 van der Weiden（1995）给出了实验"
       "结果，其中非线性补偿……（详见原文）。")

b.para("式 4.212 的静态关系可改写为")
eq("6.51")
b.para("其中 pS,max 是最大供油压力。由此可直接看出，非线性负载（及供油压力变化）可通过下式"
       "补偿：", indent=False)
eq("6.52")
b.para("然而，这些补偿技术大多主要应用于对称缸。一个例外是 Boes（1995），他认识到对差动缸的"
       "负载补偿，缸面积比 α 起重要作用。对差动缸，相应的（更复杂的）关系可由式 4.196 和 4.197"
       "（无泄漏项）导出，对 u ≥ 0 为", indent=False)
eq("6.53")
b.para("对 u < 0 为", indent=False)
eq("6.54")
b.para("这种补偿技术的一个“好”副产品是系统（速度）行为变得对称。", indent=False)
b.label("6.4.3.3　控制器增益的切换（Switching of Controller Gains）")
b.para("尽管不如液压执行器的流量非线性重要，但执行器动态的位置依赖性必须就伺服系统的闭环性能"
       "加以考虑。式 4.234 和 4.235 中参数 s5 和 s6 的位置依赖性使执行器的刚度（以及固有频率）"
       "位置相关；见图 6.17。例如，对称执行器在中位刚度最低；差动（非对称）缸的情形更复杂。")
fig("6.17")
b.para("这种刚度对闭环性能的影响还取决于控制策略，即反馈增益设置：若位置控制是主要目标，会选"
       "相对高的位置反馈增益，此时位置反馈增益对稳定性至关重要，反馈增益和可达性能受执行器"
       "刚度限制（高刚度允许高反馈增益，故鲁棒反馈设计基于最低刚度，如对称执行器的中位）；若"
       "压力控制是主要目标，压差增益应选得尽可能高。注意，若 HSS 中使用差动缸，则必须考虑"
       "控制器增益的切换。")
b.label("6.4.3.4　差动缸的速度补偿（Velocity Compensation for Differential Cylinders）")
b.para("对 HSS，希望速度（绝对值）与阀控制输入的符号无关（但绝对值相同）；见 Saffe（1986）和 "
       "Lemmen（1995）。由于对含差动缸的系统这并非天然成立，可能需要补偿以强制实现期望的"
       "（对称）速度行为，即")
eq("6.55")
b.para("这意味着须施加校正增益", indent=False)
eq("6.56")
b.para("为计算校正增益，必须以足够精度已知或估计参数 pS、pT 和 pL = FL/Ap。一个用节流-单向阀"
       "实现这种速度补偿的实用方案示于图 6.18。注意，这里讨论的补偿功能已包含在 6.4.3.2 节给出"
       "的非线性流特性补偿中。", indent=False)
fig("6.18")
b.h3("6.4.4　经典方法的结论与缺点", "Conclusions and Drawbacks of Classical Approaches")
b.para("经典控制方法的常见设计步骤是：首先由非线性模型的线性化（或通过直接辨识线性模型）导出"
       "执行器和负载的积分模型；然后对线性模型应用基于模型的控制设计技术。这种方法有若干缺点"
       "（Heintze, 1997）：")
b.para("1.　在基于线性模型的控制设计中，线性化模型显然只在工作点附近的小区域内有效，故线性"
       "控制设计方法无法保证闭环在整个运行范围内有相同的动态行为。", indent=False)
b.para("2.　任意极点配置或最优控制需要完整的参数知识，这对与系统液压部分相关的若干参数可能"
       "特别成问题（见第 4、5 章）。", indent=False)
b.para("3.　按 6.3 节实现并针对特定情形整定全状态反馈控制器，对非控制专家而言往往不透明，故"
       "工业场合的实际应用可能困难。", indent=False)
b.para("为应对这些困难，一些作者提出了鲁棒控制设计方法（H∞）；如 Lambrechts（1994）和 Tunay "
       "等（2001）。然而，由于体积模量等的不确定性可达 50% 或更多，这些方法会产生非常保守的"
       "控制器；而且对 H∞ 控制，这些设计过程和所得控制器如此复杂，以致这种鲁棒设计方法的实际"
       "应用值得怀疑（Heintze, 1997）。下面各节介绍液压驱动系统控制设计的一些近期方法，它们"
       "可避免上述缺点。")
b.h2("6.5　反馈线性化控制", "Feedback Linearising Control")
b.para("反馈线性化是一种非线性控制设计方法，过去二十年引起了极大研究兴趣。该方法的一般思想是"
       "把非线性系统动态代数地变换为（完全或部分）线性的，从而可应用线性控制技术。这与常规"
       "线性化（即雅可比线性化）完全不同：反馈线性化通过精确的状态变换和反馈实现，而非通过"
       "动态的线性近似。其最简单形式是抵消非线性系统中的非线性，使闭环动态呈线性形式（Slotine "
       "and Li, 1991）。")
b.para("完全反馈线性化通常称为精确线性化或输入-状态线性化（经反馈）。若不满足精确线性化所需的"
       "（限制性）条件，就无法经反馈和坐标变换得到线性可控系统；然而，总可以实现一个分解系统，"
       "其中两个分量子系统之一是线性的——此时可至少寻求使线性子系统维数最大化的反馈和坐标"
       "变换（Isidori, 1995）。这直接引向输入-输出线性化的概念。非线性控制系统（尤其精确线性化）"
       "设计与分析的详尽理论发展见 Isidori（1995）和 Slotine 与 Li（1991），6.5 节的定义和定理"
       "取自其中。")
b.h3("6.5.1　反馈线性化与友形", "Feedback Linearisation and the Companion Form")
b.para("反馈线性化的最简单形式是抵消非线性系统中的非线性，然后强加闭环系统的期望线性动态。"
       "若非线性系统处于所谓的友形（companion form）或可控性标准形，这便易于实现，即（对 SISO "
       "系统）")
eq("6.57")
b.para("该方程可由状态空间模型表示", indent=False)
eq("6.58")
b.para("对可控性标准形描述的系统，可通过施加控制输入", indent=False)
eq("6.59")
b.para("抵消非线性，得到简单的输入-输出关系（多重积分器形式）", indent=False)
eq("6.60")
b.para("v(t) 是新输入（也称虚拟或人工控制输入），可由极点配置法确定；见 6.5.3.3 节。", indent=False)
b.h3("6.5.2　输入-输出线性化的直观概念", "Intuitive Concept of Input-Output Linearisation")
b.para("考虑由单输入单输出（SISO）状态空间表示描述的非线性系统")
eq("6.61")
b.para("其中 u 是输入，y 是输出，x 是状态向量，f(x)、g(x)、h(x) 是非线性函数。这种结构的系统称为"
       "控制仿射或控制线性的。所谓输入-输出线性化方法的直观而简单的基础是找到系统输出 y(t) 与"
       "控制输入 u(t) 之间的直接（且简单）关系。", indent=False)
b.para("为生成这种直接关系，把输出方程 y = h(x) 微分足够多次，直到输入 u 显式出现。一旦获得 u "
       "与 y 之间的显式关系", indent=False)
eq("6.62")
b.para("就把相应的输出导数置为新输入 v，并就 u 求解该关系，得到输入-输出线性化的控制律。更"
       "精确地说，从对输出 y(t) 关于时间求导开始，应用链式法则得", indent=False)
eq("6.63")
b.para("为给表达式 ∂h/∂x·f(x) 和 ∂h/∂x·g(x) 及更高阶导数提供紧凑记号，使用微分几何中已知的"
       "李导数（Lie derivatives，见附录 C.2）。用此记号，式 6.63 可写为", indent=False)
eq("6.64")
b.para("若 Lg h(x) 不为零，此式给出所寻求的 u 与 y 之间的显式关系（假设 u 与 y 之间无直接馈通"
       "连接，多数实际对象通常如此）。若 Lg h(x) 对所有 x 都等于零，则须把 y(t) 再微分一次，得", indent=False)
eq("6.65")
b.para("同样，若 Lg Lf h(x) 对所有 x 等于零，则一再重复微分，直到导出", indent=False)
eq("6.66")
b.para("对某个整数 r（称为相对阶，relative degree）且 Lg Lf^(r−1) h(x) ≠ 0。式 6.66 现在显然可重排"
       "为如下形式的反馈", indent=False)
eq("6.67")
b.para("它抵消对象的非线性并导致如下形式的线性输入-输出关系", indent=False)
eq("6.68")
b.para("（系统输出 y 与新输入 v 之间），或等价地传递函数", indent=False)
eq("6.69")
b.para("例 6.1。　第一个例子是带非线性摩擦和非线性流函数的同步缸（假设体积模量恒定、腔容积"
       "恒定）；见式 4.213。该系统阶数 n=3，由下式描述（u > 0；本例忽略阀动态）：")
eq("6.70", "6.71", "6.72", "6.73")
b.para("对输出 y(t) 求导：", indent=False)
eq("6.74", "6.75")
b.para("注意 u 未出现在此式中，即 Lg h(x)=0；故把式 6.75 再次微分得", indent=False)
eq("6.76")
b.para("由于 Lg Lf h(x) 等于零，需要第三次微分：", indent=False)
eq("6.77")
b.para("其中", indent=False)
eq("6.78", "6.79", "6.80")
b.para("于是（由于输入显式出现），该系统输入-输出可线性化，相对阶 r=3。该系统的标准表示"
       "（式 6.77）示于图 6.19。", indent=False)
fig("6.19")
b.para("现在显然，如下形式的反馈")
eq("6.81")
b.para("抵消系统的非线性并导致输出 y 与新（虚拟）输入 v 之间的线性关系", indent=False)
eq("6.82")
b.para("利用这一线性关系，可轻易使系统输出呈现期望行为：为此，虚拟信号 v 可选为 y 和 yref（及其"
       "时间导数）的适当函数。注意，本例所考虑的系统是精确可线性化的（因为 r=n 成立）。", indent=False)

b.h3("6.5.3　反馈线性化的形式化理论", "Formalised Theory of Feedback Linearisation")
b.label("6.5.3.1　单输入单输出系统的输入-输出线性化")
b.para("再次考虑式 6.61 的状态空间表示。反馈线性化分析的出发点是相对阶的形式定义：")
b.para("定义 6.1（相对阶）。　若对所有 x ∈ Ω（或在 x0 的某邻域内），非线性系统式 6.61 满足 "
       "(i) Lg Lf^i h(x) = 0（∀ i < r−1），(ii) Lg Lf^(r−1) h(x) ≠ 0，则称它在区域 Ω（或在点 x0）"
       "具有相对阶 r。", indent=False)
b.para("对线性系统，该记号化为人们熟悉的相对阶定义——极点数超过零点数之差。注意，相对阶在"
       "某些点 x0 可能无定义：当输入系数 Lg Lf^(r−1) h(x) 在 x0 为零、但在任意接近 x0 的某些点 x "
       "不为零时，便会发生。", indent=False)
b.para("定理 6.1（标准形）。　当相对阶 r（良好）定义且 r < n 时，非线性系统式 6.61 可用坐标变换")
eq("6.83")
b.para("（其中 ti(x) 任意）变换为所谓的标准形（normal form，有时称为 Byrnes-Isidori 标准形）", indent=False)
eq("6.84")
b.para("在标准形方程上设置状态反馈控制律", indent=False)
eq("6.85")
b.para("（其中 v 是外部参考输入），便得到系统", indent=False)
eq("6.86")
b.para("显然，该系统被分解为一个维数为 r 的线性子系统（或 r 个积分器的链），它只负责输入-输出"
       "行为；以及一个维数为 n−r 的可能非线性的子系统，但其行为是“不可观测”的，即不影响"
       "输出；见图 6.20。", indent=False)
fig("6.20")
b.para("线性化反馈可用刻画系统原始描述（式 6.61）的函数 f(x)、g(x)、h(x) 表示，见下述定理。")
b.para("定理 6.2（输入-输出线性化）。　当相对阶 r（良好）定义且 r < n 时，状态反馈", indent=False)
eq("6.87")
b.para("把非线性系统（式 6.61）变换为这样一个系统：其输入-输出行为与具有式 6.68 输入-输出关系"
       "和式 6.69 传递函数的线性系统相同。", indent=False)
b.para("在 r = n 的特殊情形下，非线性系统（式 6.61）是精确可线性化（或输入-状态可线性化）的。"
       "三元组 {f(x), g(x), h(x)} 具有相对阶 n 的条件集由下述定理给出：")
b.para("定理 6.3（精确线性化）。　非线性系统式 6.61 精确可线性化，当且仅当存在区域 Ω（或点 x0）"
       "使下列条件成立：(a) 向量场 {g, adf g, …, adf^(n−1) g} 线性无关，即矩阵 [g  adf g  …  "
       "adf^(n−1) g] 满秩（可控性条件）；(b) 集合 {g, adf g, …, adf^(n−2) g} 在 Ω 内（或在 x0 的"
       "邻域内）是对合的（involutive，对合性条件）。", indent=False)
b.para("对合性意味着：若对集合中任意一对向量场作李括号（Lie bracket），则所得向量场可表示为"
       "原向量场集合的线性组合；见附录 C.3。执行精确线性化（或输入-状态线性化）的方法可见 "
       "Slotine 与 Li（1991:241-242）。")
b.label("6.5.3.2　内动态与零动态（Internal and Zero Dynamics）")
b.para("如上所述，输入-输出线性化把非线性系统分解为一个外部（线性输入-输出）部分和一个内部"
       "（不可观测）部分。由于控制设计必须顾及整个系统，两部分都必须稳定。外部部分的稳定性"
       "因可控性标准形（式 6.86）而易于保证，故剩下须研究内动态的稳定行为。考虑式 6.86，定义"
       "向量")
eq("6.88")
b.para("得到", indent=False)
eq("6.89")
b.para("则与输入-输出线性化相关的内动态恰好对应于标准形式 6.89 中最后 (n−r) 个方程 q = q(ξ, η)。"
       "当输入和初始条件被选得约束输出恒等于零时，内动态称为零动态（zero dynamics），写为", indent=False)
eq("6.90")
b.para("零动态的重要性由下述定理凸显。", indent=False)
b.para("定理 6.4（输入-输出可线性化系统的镇定）。　假设非线性系统（式 6.61）具有（良好）定义的"
       "相对阶 r（r < n），且 (n−r) 维零动态系统（式 6.90）的平凡平衡点局部渐近稳定。在这些条件"
       "下，控制律（式 6.85）产生一个局部渐近稳定的闭环系统。", indent=False)
b.para("该结果表明，只要零动态渐近稳定，输入-输出线性化控制律确实能局部（而非全局）镇定整个"
       "系统。基于部分反馈的全局镇定方法是把控制问题视为标准的 Lyapunov 控制器设计问题，但"
       "因把系统置于标准形使部分动态线性而得到简化（Slotine and Li, 1991）。")
b.label("6.5.3.3　输入-输出可线性化系统的控制器设计")
b.para("一旦非线性系统被变换为具有线性输入-输出关系的系统，控制设计便可用所有著名而有力的"
       "线性设计技术完成。重要的是，设计控制律无需显式知道系统标准形的表达式，只需知道系统"
       "具有在 η=0 处局部（或全局）渐近稳定平衡的零动态。一种简单的控制设计方法是线性极点"
       "配置法，即把新（人工）输入 v 置为")
eq("6.91")
b.para("其中 ν 是新外部输入（参考信号 yref 和输出 y 的任意函数）。系数 ki 选得使多项式", indent=False)
eq("6.92")
b.para("的所有根严格位于左半平面。由式 6.87，实际控制输入可写为", indent=False)
eq("6.93")
b.para("其中可包含如简单线性输出反馈 v = K(yref − y)，得到图 6.21 所示结构。此外，简单极点配置"
       "控制器可扩展到渐近跟踪控制任务（Isidori, 1995）：", indent=False)
b.para("定理 6.5（渐近输出跟踪）。　假设非线性系统式 6.61 具有（良好）定义的相对阶 r（r < n），且"
       "给定的期望轨迹", indent=False)
eq("6.94")
b.para("光滑有界。再假设方程", indent=False)
eq("6.95")
b.para("的解 ηref 存在、有界且一致渐近稳定。选择常数使式 6.92 的多项式所有根严格在左半平面。"
       "则使用控制律", indent=False)
eq("6.96")
b.para("整个状态保持有界，跟踪误差指数收敛于零。", indent=False)
fig("6.21")
b.para("如图 6.21 清楚所示，反馈线性化得到级联控制（或内/外环控制）方案：用于反馈线性化的内环"
       "把系统动态变换为一组 SISO 非耦合积分器；外环用于配置所得线性系统的极点，使受控系统"
       "实现期望行为，而与原系统动态无关。机器人文献中著名的内/外环控制例子是计算力矩控制"
       "（computed torque control）。积分作用也可如 6.3.3 节所述纳入。")
b.para("迄今所述反馈线性化律假设系统所有状态变量可用。对 HSS 的许多应用，只有活塞位置可直接"
       "测量，其他状态变量须用状态观测器估计（见 6.10 节）。这导出图 6.22 所示的完整控制方案。")
fig("6.22")
b.h3("6.5.4　对液压伺服系统模型的应用", "Application to Hydraulic Servo-system Models")
b.para("视作为输入-输出线性化基础的 HSS 模型复杂度而定，可导出不同（或多或少复杂的）控制律。")
b.label("6.5.4.1　对简化 HSS 模型的应用")
b.para("首先，对子系统“压力动态”（4.4.1 节式 4.196，n=2）以输出方程")
eq("6.97")
b.para("进行输入-输出线性化。对该式求导并代入压力动态的状态方程，得", indent=False)
eq("6.98")
b.para("由于式 6.98 表示 u 与 y 之间的显式关系，可重排给出状态反馈线性化控制律（对 u ≥ 0）", indent=False)
eq("6.99")
b.para("u < 0 的情形的控制律直截了当地为", indent=False)
eq("6.100")
b.para("为实际实现线性化控制律，压力 x3 = pA、x4 = pB 以及活塞速度 x2 = ẋp 必须可通过测量或估计"
       "获得。对所有工况，从新输入 v 到 y = Ap(x3 − αx4) 我们得到精确线性的输入-输出行为。于是"
       "负载状态方程取如下形式", indent=False)
eq("6.101")
b.para("为控制活塞位置，可使用式 6.99、6.100 的控制律以及如简单输出反馈", indent=False)
eq("6.102")
b.para("控制律的解释。　总控制律由直接反馈（式 6.102）与速度补偿、泄漏补偿和阀流非线性抵消"
       "相结合组成：", indent=False)
eq("6.103")
b.para("速度补偿用于补偿活塞速度对执行器腔质量平衡的贡献；换言之，速度补偿消除了执行器压力"
       "动态与活塞-负载动态之间的耦合。泄漏补偿在某些情形下可提高控制器性能，但代价是更多的"
       "模型知识和更严格的稳定性条件。", indent=False)
b.label("6.5.4.2　对消去速度的简化 HSS 模型的应用")
b.para("再次考虑子系统“压力动态”（4.4.1 节式 4.196 和 4.197）。活塞速度不能直接测量，观测器"
       "又可能因传感器和量化噪声而失效。因此，希望寻找一种不出现速度的压力动态描述。利用 Kugi "
       "等（1999）给出的思想，在体积模量恒定且两腔相等（即 E′A(x1)=E′B(x4)=:E′）的假设下，"
       "速度的消去可通过如下变换实现：")
eq("6.104")
b.para("于是式 4.196 和 4.197 可改写为（对 u ≥ 0）", indent=False)
eq("6.105")
b.para("以及（对 u < 0）", indent=False)
eq("6.106")
b.para("注意，式 6.104 不过是腔压力 pA、pB 加上因腔容积变化而产生的压力偏差。现在再次对", indent=False)
eq("6.107")
b.para("如上所述进行输入-输出线性化，得到控制律", indent=False)
eq("6.108")

b.para("对 u < 0 的控制律为（其中不含活塞速度）")
eq("6.109")
b.para("此情形下，从新输入 v 到 y = Ap(x3 − αx4) 我们同样得到（精确）线性的输入-输出行为，但负载"
       "状态方程取略有不同的形式", indent=False)
eq("6.110")
b.para("带非线性元素", indent=False)
eq("6.111")
b.para("可立即看出，用压力变换把活塞速度从控制律中移除，代价是与式 6.101 相比，式 6.110 中出现"
       "非线性 f(x1)。Kugi（2001）利用著名的 Hurwitz 和 Popov 判据证明：只要满足不等式条件", indent=False)
eq("6.112", "6.113")
b.para("式 6.108 和 6.109 的控制律就在整个运行域上、对体积模量 E′ > E′min 的所有可能值保证稳态点 "
       "(x1,ref) 的渐近稳定。此时可使用输出反馈", indent=False)
eq("6.114")
b.label("6.5.4.3　对更复杂 HSS 模型的应用")
b.para("当然，输入-输出线性化也可基于更复杂的模型（式 4.181）导出，如 Lemmen 与 Bröcker"
       "（1999, 2000）所示。由于设计这种控制器所需的计算非常复杂，手工推导既繁琐又易出错，因此"
       "更方便的是使用基于 Maple 或 Mathematica 等符号软件包的 CACSD 工具箱。式 4.181 的"
       "输入-输出线性化控制器（忽略伺服阀动态和泄漏流）为")
eq("6.115")
b.para("其中分子计算为", indent=False)
eq("6.116")
b.para("带", indent=False)
eq("6.117")
b.para("导数 F′f(x2) ≡ dFf/dx2 可由对摩擦的求导得到", indent=False)
eq("6.118")
b.para("带光滑近似", indent=False)
eq("6.119", "6.120")
b.para("因子 γ 决定近似质量；见图 6.23。式 6.115 的分母（对 u ≥ 0）计算为", indent=False)
eq("6.121")
b.para("对 u < 0 为", indent=False)
eq("6.122")
fig("6.23")
b.h3("6.5.5　基于双线性模型的反馈线性化", "Feedback Linearisation Based on Bilinear Models")
b.para("双线性模型是非线性模型的一个重要子类，因三个原因而有吸引力：(1) 双线性模型是最简单的"
       "非线性模型类型；(2) 在为许多非线性物理系统建模时，它们可视为合适的近似；(3) 双线性系统"
       "的理论已相当成熟（Mohler, 1973；Schwarz, 1991, 2000；Isidori, 1995）。此外，近年来已有"
       "若干用双线性模型对液压系统建模和控制的尝试。例如，Yin（1992, 1994）基于某特殊标准形的"
       "辨识双线性模型开发了状态反馈控制器。该控制器由反馈线性化控制律和跟踪控制算法组成，是 "
       "6.5.3.3 节控制方案的特例；不可测状态变量用最优双线性观测器由伺服系统的位置测量确定"
       "（图 6.24）。")
b.para("对处于可控性标准形的 SISO 双线性系统")
eq("6.123")
b.para("即", indent=False)
eq("6.124")
b.para("可用如下状态反馈律（见式 6.59）抵消非线性：", indent=False)
eq("6.125")
fig("6.24")
b.para("然而，此情形下唯一的非线性在输入部分，故只需考虑（Yin, 1992, 1994）")
eq("6.126")
b.para("事实上，把式 6.126 代入式 6.123 得到线性状态空间描述", indent=False)
eq("6.127")
b.para("因此，所考虑双线性系统的控制器设计任务变成了线性系统（式 6.127）的控制器设计。对 HSS"
       "（归一化输入），由于约束 |u| < 1，有 |kt·x| < 1，故反馈线性化律取如下形式", indent=False)
eq("6.128")
b.para("然而实际上，由于对 u 的约束，不可能在整个输入信号变化范围内精确线性化非线性对象动态。"
       "对第 7.1 节所考虑阀-缸组合的实验结果表明，上述反馈线性化律只在 |w| < 0.5 时有效"
       "（Yin, 1994）。", indent=False)
b.h2("6.6　类似反馈线性化的方法", "Approaches Similar to Feedback Linearisation")
b.para("许多研究者已从反馈线性化、输入-输出线性化框架或类似方法开发了非线性控制器，如 Hahn 等"
       "（1994）、Del Re and Isidori（1995）、Alleyne（1996）、Heintze（1997）、Bernzen"
       "（1999a）、Sohl and Bobrow（1999）、Tunay 等（2001）。本节给出并讨论其中一些方法。")
b.h3("6.6.1　直接逆控制", "Direct Inverse Control")
b.para("控制非线性系统最早为人熟知的选择之一是用逆动态模型充当控制器。若该模型表示精确逆，则"
       "它产生的控制输入将在一个采样间隔内把系统输出驱动到参考值，即得到一个无差拍（dead-beat）"
       "控制器（Åström and Wittenmark, 1990）。然而一般而言，由于若干严重的不便，直接逆控制的"
       "实际意义被认为有限：用这种控制实现对参考变化的快速响应，通常导致鲁棒性差、对噪声和"
       "高频扰动高度敏感，且常遇到非常活跃的控制信号。若逆模型不稳定（线性情形下对应于右半"
       "平面极点），则必须预料闭环系统会变得不稳定；遗憾的是，这种情形在实践中相当频繁，HSS "
       "即如此（见 Heintze, 1997 对直接逆控制的分析）。他指出，该控制器会试图进行零极点对消，但"
       "由于参数不确定性（导致如不完美的速度补偿）该对消并不发生，从而可能产生虚轴附近的零点"
       "和相当欠阻尼的共轭复极点对。")
b.h3("6.6.2　级联负载压力（负载力）控制", "Cascade Load Pressure (Load Force) Control")
b.para("液压执行器的级联 FL（或等价地 pd）控制策略相对较新，最早由 Sepehri 等（1990）提出，并由 "
       "Heintze 与 van der Weiden（1995）、Heintze 等（1995）和 van Schothorst（1997）加以详尽"
       "阐述和形式化。较近期，Bernzen（1999a）和 Sohl 与 Bobrow（1999）导出了类似方法（基于"
       "输入-输出线性化）。")
b.label("6.6.2.1　级联负载力（FL）控制导论")
b.para("基本控制结构。　如图 6.25 所示，级联 FL 控制由两个回路组成。首先，内环控制（即实际的"
       "执行器控制回路）的目标是用输入-输出线性化控制执行器的压差力 FL，而与负载的运动无关——"
       "这使执行器具有力发生器的性质。其次，外环控制关注执行器负载的（镇定）控制和外力 Fext 的"
       "补偿。")
fig("6.25")
b.para("内环控制。　如 6.5.4.1 节（见式 6.103）所述，内环控制（即输入-输出线性化反馈）由速度补偿、"
       "泄漏补偿和阀流非线性抵消组成。在完美速度补偿的情形下，实现压力动态与负载动态的完全"
       "解耦。对简单线性（三阶）模型，该压力动态为一阶，其带宽由比例反馈增益 KpL 决定；随后可"
       "施加高增益压差反馈以塑造压力动态。因此，内环级联 FL 控制器使执行器具有力发生器的性质，"
       "执行器压差力 FL 以一阶动态跟随期望压差力 FL,ref，与活塞运动无关。")
b.para("外环控制。　事实上，外环设计只是一个标准的机械运动控制问题，其中执行器在某频率范围内"
       "充当力发生器。为实现位置跟踪，期望力应选为（Bernzen, 1999a）")
eq("6.129")
b.para("其中 Ff(ẋp) 是缸中摩擦力的估计，结合简单比例反馈", indent=False)
eq("6.130")
b.para("给出一阶闭环行为", indent=False)
eq("6.131")
b.para("Sohl 与 Bobrow（1999）也提出了如下形式的反馈", indent=False)
eq("6.132")
b.para("它保证力的指数镇定，并导出闭环描述", indent=False)
eq("6.133")
b.para("然而，外环控制设计中不仅可用这种简单反馈方案，还可用更复杂的控制技术。例如，Heintze 等"
       "（1995）和 Heintze（1997）成功地把非线性滑模控制用作外环控制策略。基于输入-输出线性化的"
       "级联负载力控制的整个算法总结于图 6.26。")
fig("6.26")

b.label("6.6.2.2　线性级联 FL 控制")
b.para("这种控制器的推导通常基于带对称执行器的液压伺服系统的线性化三阶模型（形如式 4.252）；"
       "对差动执行器可用式 4.250。把受控执行器视为力发生器、把负载视为轻阻尼惯性，所示外环"
       "反馈使系统具有质量-弹簧-阻尼系统的性质。该系统的极点可由分别代表刚度和阻尼常数的"
       "增益 K1 和 K2 配置。因此，从相互作用的角度看，受控执行器对外部输入的响应如同一个其特性"
       "已被控制修改的物理系统，这通常称为阻抗控制（impedance control）（Heintze, 1997）。")
b.label("6.6.2.3　若干控制方案的比较")
b.para("与内环采用直接逆控制相比，这种级联 FL 控制器更易设计、鲁棒性更好，因为逆动态控制器"
       "基于对（不确定且位置相关的）液压部分动态的对消，故对控制设计阶段所用不准确模型敏感。"
       "把线性级联 FL 控制策略（外环控制按图 6.27）与 Tafazoli 等（1996）的控制器、以及图 6.6 的"
       "状态反馈策略比较，所有策略似乎几乎等价。研究带外环反馈的级联 FL 控制策略的反馈律可进一"
       "步阐明这一点（van Schothorst, 1997:217）：")
eq("6.134")
b.para("其中 Kp = KpL·K1，Kx = KpL·K2 − Kvc。显然，唯一的区别是一个额外的速度前馈项 Kvc·ẋref 和"
       "一个泄漏补偿项 Klc·pL，后者已在 6.4.1 节简要讨论。", indent=False)
fig("6.27")
b.h2("6.7　模糊控制", "Fuzzy Control")
b.para("模糊逻辑可以多种方式用于控制目的。图 6.28 给出一些备选方案。其中，模糊状态反馈控制和"
       "模糊模型预测控制将进一步讨论，因为二者都特别利用了 Sugeno 型模糊模型的优点。模糊状态"
       "反馈控制器直接建立在局部模型概念之上，使用并扩展了“并行分布补偿器”（parallel "
       "distributed compensator）的思想（Wang 等, 1995）；模糊模型预测控制（FMPC）则利用 "
       "Sugeno 型模型出色的预测能力。")
fig("6.28")
b.h3("6.7.1　模糊状态控制", "Fuzzy State Control")
b.label("6.7.1.1　引言")
b.para("Sugeno 型模型基于工况（regime）的系统分解思想也可用于控制。每个工况被视为一个单独的"
       "动态系统，为之设计局部控制器；然后在每一时刻，所有局部控制器被加权叠加以给出全局控制"
       "作用。已提出 PID 控制器和状态反馈控制器作为局部控制器。对状态反馈控制器，这种基于工况"
       "的分解方法也称为“并行分布补偿器”。进一步考虑状态反馈控制器，因为它们有以下优点：")
b.bullet("可迁移线性状态反馈控制器设计的方法。")
b.bullet("死区时间易于在状态空间模型中表示。")
b.bullet("即便对特性不同的系统，也可能采用统一的控制设计步骤。")
b.bullet("已发展出稳定性分析和稳定控制设计工具（基于 Lyapunov 或 LMI 的方法）。")
b.para("下面引入一个非线性模糊状态反馈控制器。该控制器建立在 5.4.2.4 节引入的 Sugeno 型模型"
       "（模糊状态空间形式）结构之上。每个局部模型中的常数项需特别考虑，因为它阻碍了把线性"
       "设计方法直接迁移到局部系统。提出一个自适应设定点滤波器，以消除非积分过程闭环特性的"
       "稳态误差。该方法基于 Kroll 等（2000）提出的概念。")
fig("6.29")
b.label("6.7.1.2　控制算法")
b.para("所提出的模糊状态控制器由四个元素组成（也勾画于图 6.29）：(1) 状态重构模块——由过程"
       "输入和输出重构过程状态；(2) 局部模型激活估计器——通过评价相应隶属函数确定 c 个局部"
       "系统中每个的当前激活；(3) 模糊状态反馈控制器——由按当前激活加权的给定局部增益计算"
       "所得全局反馈增益；(4) 设定点滤波器——消除非积分过程情形下因状态反馈控制器的比例性质"
       "而残留的稳态误差。模糊状态反馈控制器用过程状态计算控制信号：")
eq("6.135")
b.para("第二项 kᵀ(k)x(k) 是实际的状态反馈，第一项 V(k)yref(k) 是缩放的参考值。过程状态也可由"
       "过去输入和输出计算（称为“状态重构”，Isermann, 1987），无需如观测器那样使用额外的"
       "动态系统。若状态向量由延迟输入和输出组成，可导出一个非最小实现，使状态可直接由过去"
       "输入和输出构造而无需进一步计算：", indent=False)
eq("6.136")
b.para("全局反馈控制器的增益由按相应第 i 个工况的激活 μi(k) 加权的 c 个局部增益组成：")
eq("6.137")
b.para("c 个模糊基函数 μi(k) 与底层辨识模糊模型的相同，这一恒等性是“并行分布补偿器”概念的"
       "关键元素之一。所提出的比例模糊状态反馈控制器对非积分特性过程会带来稳态误差。下面使用"
       "自适应设定点滤波器来消除该误差，它缩放参考使可能的稳态误差被消除。设定点滤波器的推导"
       "从把 Sugeno 型模糊系统等价描述为时变仿射系统开始：", indent=False)
eq("6.138")
b.para("其中", indent=False)
eq("6.139")
b.para("注意，在所选表示中向量 c 是时不变的。与线性理论类似（另见 6.3.3.1 节），强制实现", indent=False)
eq("6.140")
b.para("（在稳态情形下）的设定点滤波器为", indent=False)
eq("6.141")
b.para("该滤波器需要知道稳态量，而这又需要对给定任意参考 yref,∞ 已知 u∞ 和 y∞，其确定需求解一个"
       "非线性隐式方程。为避免这样做，可使用自适应设定点滤波器，它在每一时刻由直接可用的 "
       "A(k)、b(k)、k(k)、ξ(k) 以及 u(k) 和 y(k) 计算校正因子 V(k)：", indent=False)
eq("6.142")
b.para("实践经验表明，对给定的 yref,∞，V(k) 最终收敛到 V∞。图 6.30 总结了控制算法。")
fig("6.30")
b.label("6.7.1.3　设计参数的选择")
b.para("控制器设计分两个阶段进行。第一阶段遵循“并行分布补偿器”的思想，把所有局部系统彼此"
       "孤立地考虑，进行统一的基于极点配置的状态反馈控制设计。第二阶段在考虑局部系统间耦合、"
       "并允许局部系统非统一闭环特性的情况下重新整定控制器，用数值优化方法进行。为在第一阶段"
       "配置离散时间闭环极点，可借助线性理论。Ackermann（1985）建议把极点配置在“良好稳定域”"
       "内，它由以下边界包围：")
b.bullet("稳定边界（原点周围的单位圆）减去安全裕度 δ > 0。")
b.bullet("两条恒定最小阻尼 Dmin 的对数螺线：z1,2 = r·exp(±jφp)，r = exp(−φp·Dmin/√(1−Dmin²))。")
b.bullet("原点周围的最小距离 E > 0，以限制控制作用。")
b.para("图 6.31 勾画了这些边界和所得“良好稳定域”（6.3.2 节给出了连续时间情形的相应讨论）。")
fig("6.31")
b.para("每个局部系统的闭环极点配置如下：源于用相应阶数增加来对死区时间建模的 τ 个原点极点保持"
       "不变；源于状态中输入相关部分的 m−1 个原点极点（由所选非最小实现导致）也不改变；（核心）"
       "动态系统的 n 个极点配置如下：按 Ackermann（1985）的建议，把一对主导极点配置在最小阻尼 "
       "Dmin≈0.7 的螺线上，实部按关于闭环行为的动态要求选择；为简单起见，n−2 个隐性极点全部"
       "置于原点。所有局部系统设置相同的主导闭环极点。在第二阶段，用数值方法整定全局闭环系统"
       "行为：改进潜力在于被忽略的局部系统耦合以及去除所用的统一闭环极点配置。以受控变量与"
       "参考之间均方误差最小为目标，获得了良好结果。")

# ---- (more sections appended in subsequent passes: 6.7.2 / 6.8 ...) ----

os.makedirs("parts", exist_ok=True)
b.save("parts/ch06.docx")
print("Saved parts/ch06.docx (WIP through 6.7.1)")
